import { ModulePlaceholder } from "@/components/dashboard/dashboard"

export default function HealthPage() {
  return (
    <ModulePlaceholder
      eyebrow="Body telemetry"
      title="Health"
      description="A future WHOOP-connected workspace for sleep, recovery, strain, HRV, workouts, and AI reflections on energy."
    />
  )
}
