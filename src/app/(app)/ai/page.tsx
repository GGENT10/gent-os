import { ModulePlaceholder } from "@/components/dashboard/dashboard"

export default function AIPage() {
  return (
    <ModulePlaceholder
      eyebrow="Typed operator"
      title="AI"
      description="The chat surface will call typed tools, mutate structured state, and leave an audit trail for every meaningful operation."
    />
  )
}
