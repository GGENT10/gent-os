import { ModulePlaceholder } from "@/components/dashboard/dashboard"

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      eyebrow="Private access"
      title="Settings"
      description="Identity, allowed email, model preferences, workspace modules, and future integration controls will live here."
    />
  )
}
