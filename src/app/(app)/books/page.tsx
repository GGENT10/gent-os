import { ModulePlaceholder } from "@/components/dashboard/dashboard"

export default function BooksPage() {
  return (
    <ModulePlaceholder
      eyebrow="Reading desk"
      title="Books"
      description="Wishlist, active reading, notes, quotes, discussions, and links between books and the rest of the operating system."
    />
  )
}
